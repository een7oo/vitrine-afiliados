{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [{ "source": "/((?!api/).*)", "destination": "/" }],
  "functions": {
    "api/scrape.js": { "regions": ["gru1"] }
  }
}
